# Box Modal

<style>{`
    .box-modal-container{
      margin-top: 32px;
    }
    
`}</style>

### CSS rule

```css
width: 100px;
padding: 10px;
border: 10px solid;
```

Once we apply the property `box-sizing: border-box;` the content width would be `60px;`
otherwise it would have been `width: 100px`;

<div className="box-modal-container">![](/box-modal.png)</div>
